projects:
  - name: "project #1"
    tasks:
      - name: "docker compose --profile grafana down"
        actions:
          - stop
          - rm
          - create
          - restart
        containers:
          - "container #1"
          - "container #2"
        volumes:
          - "volume #1"
          - "volume #2"
